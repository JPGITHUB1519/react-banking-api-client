import PropTypes from 'prop-types';

function CheckboxTableCell(props) {
  return (
    <td>
      <input type="checkbox" />
    </td>
  );
}

CheckboxTableCell.propTypes = {

};

export default CheckboxTableCell;
